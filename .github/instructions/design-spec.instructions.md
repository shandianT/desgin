---
applyTo: "specs/**,**/*.css,**/*.wxss,**/*.wxml,**/*.html,**/app.json,**/tokens.json"
---
先读 `.claude/skills/design-spec/SKILL.md` 并照做：只写 `var(--ui-*)`，数值只改 tokens.json，改完运行 `node tools/check.mjs`，按技能里的固定格式交回。
